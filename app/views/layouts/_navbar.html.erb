<div class="navbar navbar-default navbar-fixed-top">
	<div class="container">
		<div class="navbar-header">
			<a href="#home" class="navbar-brand">
				<img alt="Gabaritei" src="assets/logo_mini.png" />
			</a>
			<button class="navbar-toggle" type="button" data-toggle="collapse" data-target="#navbar-main">
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
		</div>
		<div class="navbar-collapse collapse" id="navbar-main" ng-controller="MenuController as Ctrl">
			<ul class="nav navbar-nav">
				<%= render 'layouts/navlist' %>
			</ul>
			<ul class="nav navbar-nav navbar-right">
				<li class="hidden-xs hidden-sm" ng-show="Ctrl.user">
					<p class="navbar-text" style="color: white">{{ ('menu.hello' | translate) + Ctrl.user.first_name + "!" }}</p>
				</li>
				<li>
					<a href="#" ng-click="Ctrl.logout()">
						<span class="sub_icon glyphicon glyphicon-off"></span>
						<span translate>menu.logout</span>
					</a>
				</li>
			</ul>
		</div>
	</div>
</div>
